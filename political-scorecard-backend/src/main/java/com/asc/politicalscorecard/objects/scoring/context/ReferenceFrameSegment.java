package com.asc.politicalscorecard.objects.scoring.context;

// Represents a segment of a reference frame, such as if the reference frame is a time period, a segment could be per-year.
// This is used to narrow down the scope of data for a context.
public class ReferenceFrameSegment {
    // The name of the segment.
    private String name;
}